## 计划清单
- [x] 理解需求并确认需要独立测试 Scene
- [x] 新增可批量操作 1000 个 Image 的基准测试脚本
- [x] 创建并保存专用测试 Scene，挂载测试入口组件
- [x] 刷新 Unity 资源并确认场景内组件挂载成功
- [x] 产出本次执行日志

## 最终完成
- 新增 `Assets/Scripts/UI/Benchmark/ImageBatchBenchmark.cs`，运行时自动生成测试 UI、1000 个 Image 样本和两种基准测试按钮
- 新增 `Assets/Scenes/ImageBenchmark.unity`，场景内已挂载 `ImageBatchBenchmarkRunner` 与 `Cyber.ImageBatchBenchmark`
- 两个按钮分别测试 `Image.enabled = false` 与 `Image.color.a = 0`，结果会显示多轮采样的 `write` 与 `frame` 开销
- 完成 Unity 刷新与场景校验，确认测试场景已可直接打开使用
- 产出本次执行日志
